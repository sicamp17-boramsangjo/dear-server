{
    "fields": {
        "createUser": {
            "required": ["userName", "phoneNumber", "password", "birthDay"],
            "allowed": []
        },
        "getUserInfo": {
            "required": ["sessionToken"],
            "allowed": []
        },
        "addQuestion": {
            "required": ["text"],
            "allowed": []
        },
        "getQuestion": {
            "required": ["questionID"],
            "allowed": []
        },
        "getTodaysQuestion": {
            "required": ["sessionToken"],
            "allowed": []
        },
        "createAnswer": {
            "required": ["sessionToken", "questionID"],
            "allowed": ["answerText", "answerPhoto", "answerVideo", "receivers"]
        },
        "getWillItem": {
            "required": ["sessionToken", "willitemID"],
            "allowed": []
        },
        "getWillItems": {
            "required": ["sessionToken"],
            "allowed": []
        }
    },
    "settings": {
        "user": {
            "pushDuration": 31536000
        }
    }
}
