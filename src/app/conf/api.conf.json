{
    "fields": {
        "createUser": {
            "required": ["userName", "phoneNumber", "password", "birthDay"],
            "allowed": []
        },
        "checkAlreadyJoin": {
            "required": ["phoneNumber"],
            "allowed": []
        },
        "login": {
            "required": ["phoneNumber", "password"],
            "allowed": []
        },
        "logout": {
            "required": ["sessionToken"],
            "allowed": []
        },
        "getUserInfo": {
            "required": ["sessionToken"],
            "allowed": []
        },
        "deleteUser": {
            "required": ["sessionToken"],
            "allowed": []
        },
        "updateUserInfo": {
            "required": ["sessionToken"],
            "allowed": ["pushDuration", "lastLoginAlarmDuration", "profileImageUrl", "deviceToken"]
        },
        "addQuestion": {
            "required": ["text"],
            "allowed": []
        },
        "getQuestion": {
            "required": ["questionID"],
            "allowed": []
        },
        "getTodaysQuestion": {
            "required": ["sessionToken"],
            "allowed": []
        },
        "createAnswer": {
            "required": ["sessionToken", "questionID"],
            "allowed": ["answerText", "answerPhoto", "answerVideo", "receivers", "mediaWidth", "mediaHeight"]
        },
        "deleteAnswer": {
            "required": ["sessionToken", "questionID", "answerID"],
            "allowed": []
        },
        "getWillItem": {
            "required": ["sessionToken", "willitemID"],
            "allowed": []
        },
        "getWillItems": {
            "required": ["sessionToken"],
            "allowed": []
        }
    },
    "settings": {
        "user": {
            "pushDuration": 31536000
        }
    }
}
