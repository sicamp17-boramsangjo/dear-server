{
    "fields": {
        "createUser": {
            "required": ["userName", "phoneNumber", "password", "birthDay"],
            "allowed": []
        },
        "getUserInfo": {
            "required": ["sessionToken"],
            "allowed": []
        },
        "addQuestion": {
            "required": ["text"],
            "allowed": []
        },
        "getQuestion": {
            "required": ["questionId"],
            "allowed": []
        }
    },
    "settings": {
        "user": {
            "pushDuration": 31536000
        }
    }
}
